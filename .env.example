# ---- Backend (Vercel env): storage + push ----
# Create KV in Vercel Storage; env vars are auto-injected. Or add manually:
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# ADMIN_SECRET=    (long secret password to view submissions at /#admin)
# ONE_SIGNAL_APP_ID=
# ONE_SIGNAL_API_KEY=

# ---- Frontend (Vercel env, prefixed VITE_) ----
# Same OneSignal App ID as above; used for "Enable push" on admin page.
# VITE_ONESIGNAL_APP_ID=

# ---- Optional: email alert on each form submit (no push needed) ----
# Get API key at https://resend.com/api-keys ; verify domain for production "from".
# RESEND_API_KEY=
# ADMIN_EMAIL=     (your email to receive alerts)
# NOTIFY_FROM_EMAIL=   (e.g. "Trusted Home Services <noreply@yourdomain.com>"; default uses Resend onboarding)

# ---- Optional: Formspree fallback if /api/submit fails ----
# VITE_FORMSPREE_REALTOR=
# VITE_FORMSPREE_QUOTE=
